/*
#ifndef _SPELLBOOK_H_
#define _SPELLBOOK_H_

class SpellBook
{
private:

protected:
	Spell **sbSpells_;

	int sbMaximumSpells_;
	int sbTotalSpells_;

public:
	SpellBook();
	SpellBook(int);

	addSpell    (Spell*);
	removeSpell (int x);
	getSpells   (int x);
};

#endif

*/

